# EM算法核心知识点整理（期末复习版）
## 一、核心名词解释与概念定义
### 1. 最大似然估计（Maximum Likelihood Estimation, MLE）
- **定义**：给定观测数据，在模型参数的所有可能取值中，找到使“观测数据出现概率最大”的参数值的估计方法。
- **核心思想**：参数值应与观测数据最契合，即最大化“似然函数”（描述参数下观测数据的概率）。
- **关键步骤**：构造似然函数→取对数似然（简化计算）→求导并令导数为0→解参数值。
- **适用场景**：数据完整、无隐变量的参数估计（如抛硬币概率、正态分布均值/方差估计）。

### 2. Jensen不等式
- **定义**：描述凸/凹函数与随机变量期望的关系，是EM算法收敛性的理论基础。
- **数学表述**：
  - 凸函数（如$f(x)=x^2$）：$f(\mathbb{E}[X]) \leq \mathbb{E}[f(X)]$（函数作用于期望 ≤ 期望作用于函数）。
  - 凹函数（如$f(x)=\ln x$）：$f(\mathbb{E}[X]) \geq \mathbb{E}[f(X)]$（函数作用于期望 ≥ 期望作用于函数）。
- **核心作用**：在EM算法中构造对数似然函数的下界，确保每次迭代后对数似然函数不减，保证算法收敛。

### 3. EM算法（Expectation-Maximization Algorithm）
- **定义**：针对含**隐变量**或**不完全数据**的概率模型，通过“迭代优化”寻找参数最大似然估计的算法。
- **核心概念**：
  - 观测数据$X$：可直接测量的数据（如混合高斯模型中的样本点）。
  - 隐变量$Z$：无法直接观测，但影响观测数据的变量（如混合高斯模型中样本所属的高斯分量）。
  - 完整数据：$(X,Z)$（观测数据+隐变量）。
- **核心思想**：将“直接最大化观测数据对数似然”的复杂问题，转化为“交替优化期望（E步）和最大化下界（M步）”的简单问题。
- **算法目标**：最大化观测数据的对数似然函数$\ln p(X;\theta)$（$\theta$为待估计参数）。

### 4. 隐变量（Latent Variable）
- **定义**：无法直接从数据中观测，但会影响观测结果的变量，是EM算法的核心处理对象。
- **示例**：
  - 混合高斯模型（GMM）：样本所属的高斯分量（如数据点1.0属于第一个高斯分量还是第二个）。
  - 隐马尔可夫模型（HMM）：不可观测的状态序列。

### 5. 高斯混合模型（Gaussian Mixture Model, GMM）
- **定义**：假设数据由多个高斯分布（分量）混合生成的概率模型，是EM算法的典型应用场景（用于聚类）。
- **核心参数**：$\theta=\{\pi_k, \mu_k, \Sigma_k\}_{k=1}^K$（$K$为高斯分量数）。
  - 混合系数$\pi_k$：第$k$个高斯分量的权重，满足$\sum_{k=1}^K \pi_k=1$。
  - 均值$\mu_k$：第$k$个高斯分量的均值向量。
  - 协方差$\Sigma_k$：第$k$个高斯分量的协方差矩阵（一维时为方差$\sigma_k^2$）。

## 二、重点知识点详解（必考）
### 1. 最大似然估计（MLE）的核心逻辑
#### （1）似然函数与对数似然函数
- 似然函数$L(\theta)$：给定参数$\theta$时，观测数据$X$出现的概率（$\theta$的函数）。
- 对数似然函数$\ell(\theta)=\ln L(\theta)$：将乘积形式的似然函数转化为求和形式，简化求导计算（对数函数单调递增，不改变极值点）。

#### （2）MLE的通用步骤
1. 确定数据分布类型（如二项分布、正态分布）。
2. 构造似然函数$L(\theta)$（观测数据的联合概率）。
3. 取对数似然$\ell(\theta)=\ln L(\theta)$。
4. 对$\theta$求导（或偏导），令导数为0。
5. 解方程得到参数$\theta$的MLE估计值。

### 2. Jensen不等式在EM算法中的作用
#### （1）核心挑战
直接最大化观测数据对数似然$\ln p(X;\theta)=\ln \int p(X,Z;\theta)dZ$（含隐变量$Z$的积分）难以计算。

#### （2）下界构造（关键步骤）
1. 引入任意分布$q(Z)$（通常取$q(Z)=p(Z|X;\theta^{(t)})$，即当前参数下隐变量的后验分布）。
2. 改写对数似然函数：  
   $$\ln p(X;\theta)=\ln \int q(Z) \cdot \frac{p(X,Z;\theta)}{q(Z)}dZ$$
3. 应用Jensen不等式（$\ln x$是凹函数）：  
   $$\ln p(X;\theta) \geq \int q(Z) \ln \frac{p(X,Z;\theta)}{q(Z)}dZ$$
4. 定义辅助函数$Q(\theta,\theta^{(t)})=\int p(Z|X;\theta^{(t)}) \ln p(X,Z;\theta)dZ$，该函数是$\ln p(X;\theta)$的下界。

#### （3）收敛性保证
EM算法通过“最大化下界$Q(\theta,\theta^{(t)})$”更新参数，确保每次迭代后$\ln p(X;\theta^{(t+1)}) \geq \ln p(X;\theta^{(t)})$，即对数似然函数单调不减，最终收敛到局部极大值。

### 3. EM算法的具体步骤（必考简答题）
#### （1）初始化
随机初始化模型参数$\theta^{(0)}$（如GMM的$\pi_k^{(0)},\mu_k^{(0)},\Sigma_k^{(0)}$）。

#### （2）E步（期望步，Expectation）
在当前参数$\theta^{(t)}$下，计算隐变量$Z$的后验分布$p(Z|X;\theta^{(t)})$，并求完整数据对数似然的条件期望，即构造$Q(\theta,\theta^{(t)})$：  
$$Q(\theta,\theta^{(t)})=\mathbb{E}_{Z|X;\theta^{(t)}}[\ln p(X,Z;\theta)]$$
- 核心输出：隐变量的后验概率（如GMM中的“责任度”$\gamma_{ik}=p(z_i=k|x_i;\theta^{(t)})$）。

#### （3）M步（最大化步，Maximization）
寻找新参数$\theta^{(t+1)}$，最大化E步构造的$Q(\theta,\theta^{(t)})$：  
$$\theta^{(t+1)}=\arg\max_{\theta} Q(\theta,\theta^{(t)})$$
- 核心输出：更新后的模型参数（如GMM的$\pi_k^{(t+1)},\mu_k^{(t+1)},\Sigma_k^{(t+1)}$）。

#### （4）收敛判断
重复E步和M步，直到参数变化量或对数似然函数增量小于预设阈值（如$|\ln p(X;\theta^{(t+1)}) - \ln p(X;\theta^{(t)})| < 10^{-6}$）。

### 4. EM算法的适用场景与优缺点
#### （1）适用场景
- 含隐变量的模型（GMM、HMM、LDA等）。
- 数据不完全（存在缺失值）的参数估计。
- 直接最大化似然函数难以计算的场景。

#### （2）优缺点
| 优点                                  | 缺点                                  |
|---------------------------------------|---------------------------------------|
| 实现简单，E步和M步计算逻辑清晰        | 仅收敛到局部极大值，依赖初始参数选择    |
| 无需处理隐变量的积分/求和，通过期望转化 | 收敛速度可能较慢（需多轮迭代）          |
| 适用于多种概率模型，通用性强          | 复杂模型的E步可能无法解析求解          |

## 三、计算题（必考重点）
### 1. 最大似然估计（MLE）计算题
#### 例题1：离散型数据（抛硬币概率估计）
##### 题目：
连续抛10次硬币，观测结果为“正、反、正、正、反、正、反、反、正、正”（共6次正面，4次反面）。假设硬币正面朝上的概率为$p$，用MLE估计$p$。

##### 解答步骤：
1. **确定分布与似然函数**：
   抛硬币结果服从二项分布，观测数据的似然函数为（6次成功“正面”，4次失败“反面”）：  
   $$L(p)=p^6 \cdot (1-p)^4$$

2. **构造对数似然函数**：
   取自然对数简化计算：  
   $$\ell(p)=\ln L(p)=6\ln p + 4\ln(1-p)$$

3. **求导并令导数为0**：
   对$p$求导：  
   $$\frac{d\ell(p)}{dp}=\frac{6}{p} - \frac{4}{1-p}$$
   令导数为0，解方程：  
   $$\frac{6}{p} = \frac{4}{1-p} \implies 6(1-p)=4p \implies 6=10p \implies p=0.6$$

4. **结论**：
   硬币正面朝上概率的MLE估计值为$\hat{p}=0.6$。

#### 例题2：连续型数据（正态分布参数估计）
##### 题目：
10名男生身高数据（单位：cm）：172, 178, 169, 180, 175, 173, 177, 181, 170, 174。假设身高服从正态分布$X \sim N(\mu, \sigma^2)$，用MLE估计$\mu$和$\sigma^2$。

##### 解答步骤：
1. **正态分布概率密度与似然函数**：
   单个数据点$x_i$的概率密度：  
   $$f(x_i|\mu,\sigma^2)=\frac{1}{\sqrt{2\pi\sigma^2}}e^{-\frac{(x_i-\mu)^2}{2\sigma^2}}$$
   10个数据的似然函数（联合概率）：  
   $$L(\mu,\sigma^2)=\prod_{i=1}^{10} f(x_i|\mu,\sigma^2)=\left(\frac{1}{\sqrt{2\pi\sigma^2}}\right)^{10} e^{-\frac{1}{2\sigma^2}\sum_{i=1}^{10}(x_i-\mu)^2}$$

2. **构造对数似然函数**：
   $$\ell(\mu,\sigma^2)=-\frac{10}{2}\ln(2\pi\sigma^2) - \frac{1}{2\sigma^2}\sum_{i=1}^{10}(x_i-\mu)^2$$

3. **估计均值$\mu$**：
   对$\mu$求偏导并令其为0：  
   $$\frac{\partial \ell}{\partial \mu}=-\frac{1}{\sigma^2}\sum_{i=1}^{10}(x_i-\mu)=0 \implies \sum_{i=1}^{10}(x_i-\mu)=0$$
   解得：  
   $$\mu=\frac{1}{10}\sum_{i=1}^{10}x_i$$
   代入数据计算：  
   $$\mu=\frac{172+178+169+180+175+173+177+181+170+174}{10}=174.9\ \text{cm}$$

4. **估计方差$\sigma^2$**：
   对$\sigma^2$求偏导并令其为0：  
   $$\frac{\partial \ell}{\partial \sigma^2}=-\frac{10}{2\sigma^2} + \frac{1}{2\sigma^4}\sum_{i=1}^{10}(x_i-\mu)^2=0$$
   解得：  
   $$\sigma^2=\frac{1}{10}\sum_{i=1}^{10}(x_i-\mu)^2$$
   计算偏差平方和：  
   $$\sum_{i=1}^{10}(x_i-174.9)^2=148.9$$
   代入得：  
   $$\sigma^2=\frac{148.9}{10}=14.89\ \text{cm}^2$$

5. **结论**：
   身高正态分布的MLE估计为$\hat{\mu}=174.9\ \text{cm}$，$\hat{\sigma}^2=14.89\ \text{cm}^2$。

### 2. EM算法计算题（高斯混合模型GMM应用）
#### 题目：
给定一维数据集$X=[1.0, 1.2, 0.8, 5.0, 5.2, 4.8]$，假设数据由2个高斯分量混合生成（$K=2$）。初始化参数为：
- 混合系数：$\pi_1^{(0)}=0.5$，$\pi_2^{(0)}=0.5$
- 均值：$\mu_1^{(0)}=1.0$，$\mu_2^{(0)}=5.0$
- 方差：$\sigma_1^{2(0)}=0.1$，$\sigma_2^{2(0)}=0.1$

执行EM算法第一轮迭代（E步+M步），计算更新后的参数$\pi_1^{(1)},\pi_2^{(1)},\mu_1^{(1)},\mu_2^{(1)},\sigma_1^{2(1)},\sigma_2^{2(1)}$。

#### 核心公式：
- 高斯概率密度（一维）：$\mathcal{N}(x|\mu,\sigma^2)=\frac{1}{\sqrt{2\pi\sigma^2}}e^{-\frac{(x-\mu)^2}{2\sigma^2}}$
- 责任度（E步）：$\gamma_{ik}=\frac{\pi_k^{(0)}\mathcal{N}(x_i|\mu_k^{(0)},\sigma_k^{2(0)})}{\pi_1^{(0)}\mathcal{N}(x_i|\mu_1^{(0)},\sigma_1^{2(0)}) + \pi_2^{(0)}\mathcal{N}(x_i|\mu_2^{(0)},\sigma_2^{2(0)})}$
- 分量样本数（M步）：$N_k=\sum_{i=1}^6 \gamma_{ik}$
- 混合系数更新：$\pi_k^{(1)}=\frac{N_k}{6}$
- 均值更新：$\mu_k^{(1)}=\frac{1}{N_k}\sum_{i=1}^6 \gamma_{ik}x_i$
- 方差更新：$\sigma_k^{2(1)}=\frac{1}{N_k}\sum_{i=1}^6 \gamma_{ik}(x_i-\mu_k^{(1)})^2$

##### 解答步骤：
### 第一步：E步（计算责任度$\gamma_{i1}$和$\gamma_{i2}$）
先计算每个数据点在两个高斯分量下的概率密度：
- 高斯1（$\mu_1=1.0$，$\sigma_1^2=0.1$）：$\mathcal{N}(x|\mu_1,\sigma_1^2)=\frac{1}{\sqrt{2\pi×0.1}}e^{-\frac{(x-1.0)^2}{2×0.1}}≈\frac{1}{0.564}e^{-5(x-1.0)^2}$
- 高斯2（$\mu_2=5.0$，$\sigma_2^2=0.1$）：$\mathcal{N}(x|\mu_2,\sigma_2^2)≈\frac{1}{0.564}e^{-5(x-5.0)^2}$

计算每个数据点的$\gamma_{i1}$和$\gamma_{i2}$（$\gamma_{i2}=1-\gamma_{i1}$，因$K=2$）：
1. $x_1=1.0$：
   - $\mathcal{N}(1.0|1.0,0.1)≈\frac{1}{0.564}e^{0}≈1.773$
   - $\mathcal{N}(1.0|5.0,0.1)≈\frac{1}{0.564}e^{-5×16}≈0$
   - $\gamma_{11}=\frac{0.5×1.773}{0.5×1.773 + 0.5×0}≈1.0$，$\gamma_{12}=0.0$
2. $x_2=1.2$：
   - $\mathcal{N}(1.2|1.0,0.1)≈\frac{1}{0.564}e^{-5×0.04}≈\frac{1}{0.564}×0.819≈1.452$
   - $\mathcal{N}(1.2|5.0,0.1)≈0$
   - $\gamma_{21}≈1.0$，$\gamma_{22}=0.0$
3. $x_3=0.8$：
   - $\mathcal{N}(0.8|1.0,0.1)≈\frac{1}{0.564}e^{-5×0.04}≈1.452$
   - $\mathcal{N}(0.8|5.0,0.1)≈0$
   - $\gamma_{31}≈1.0$，$\gamma_{32}=0.0$
4. $x_4=5.0$：
   - $\mathcal{N}(5.0|1.0,0.1)≈0$
   - $\mathcal{N}(5.0|5.0,0.1)≈1.773$
   - $\gamma_{41}=0.0$，$\gamma_{42}≈1.0$
5. $x_5=5.2$：
   - $\mathcal{N}(5.2|1.0,0.1)≈0$
   - $\mathcal{N}(5.2|5.0,0.1)≈1.452$
   - $\gamma_{51}=0.0$，$\gamma_{52}≈1.0$
6. $x_6=4.8$：
   - $\mathcal{N}(4.8|1.0,0.1)≈0$
   - $\mathcal{N}(4.8|5.0,0.1)≈1.452$
   - $\gamma_{61}=0.0$，$\gamma_{62}≈1.0$

### 第二步：M步（更新参数）
1. 计算分量样本数$N_1$和$N_2$：
   - $N_1=\gamma_{11}+\gamma_{21}+\gamma_{31}+\gamma_{41}+\gamma_{51}+\gamma_{61}=1.0+1.0+1.0+0+0+0=3.0$
   - $N_2=\sum_{i=1}^6 \gamma_{i2}=0+0+0+1.0+1.0+1.0=3.0$

2. 更新混合系数$\pi_1^{(1)}$和$\pi_2^{(1)}$：
   - $\pi_1^{(1)}=\frac{N_1}{6}=\frac{3.0}{6}=0.5$
   - $\pi_2^{(1)}=\frac{N_2}{6}=0.5$

3. 更新均值$\mu_1^{(1)}$和$\mu_2^{(1)}$：
   - $\mu_1^{(1)}=\frac{1}{N_1}(\gamma_{11}x_1+\gamma_{21}x_2+\gamma_{31}x_3+\dots)=\frac{1}{3}(1.0×1.0 + 1.0×1.2 + 1.0×0.8)=1.0$
   - $\mu_2^{(1)}=\frac{1}{N_2}(\gamma_{42}x_4+\gamma_{52}x_5+\gamma_{62}x_6)=\frac{1}{3}(1.0×5.0 + 1.0×5.2 + 1.0×4.8)=5.0$

4. 更新方差$\sigma_1^{2(1)}$和$\sigma_2^{2(1)}$：
   - $\sigma_1^{2(1)}=\frac{1}{N_1}[\gamma_{11}(1.0-1.0)^2 + \gamma_{21}(1.2-1.0)^2 + \gamma_{31}(0.8-1.0)^2]=\frac{1}{3}(0 + 0.04 + 0.04)=0.0267$
   - $\sigma_2^{2(1)}=\frac{1}{N_2}[\gamma_{42}(5.0-5.0)^2 + \gamma_{52}(5.2-5.0)^2 + \gamma_{62}(4.8-5.0)^2]=\frac{1}{3}(0 + 0.04 + 0.04)=0.0267$

### 第一轮迭代结果：
- 混合系数：$\pi_1^{(1)}=0.5$，$\pi_2^{(1)}=0.5$
- 均值：$\mu_1^{(1)}=1.0$，$\mu_2^{(1)}=5.0$
- 方差：$\sigma_1^{2(1)}≈0.0267$，$\sigma_2^{2(1)}≈0.0267$

### 结论：
第一轮迭代后，均值未变，但方差缩小（更贴合数据分布）。继续迭代会使参数收敛到稳定值（如$\sigma_1^2≈0.04$，$\sigma_2^2≈0.04$）。

## 四、核心知识点总结
| 知识点         | 核心要点                                                                 |
|----------------|--------------------------------------------------------------------------|
| MLE            | 无隐变量时的参数估计，步骤：构造似然→对数似然→求导→解方程                  |
| Jensen不等式   | 凹函数$\ln x$满足$f(\mathbb{E}[X]) \geq \mathbb{E}[f(X)]$，为EM构造下界    |
| EM算法         | 含隐变量时的参数估计，E步求隐变量后验，M步最大化期望对数似然，对数似然不减 |
| GMM-EM应用     | E步算责任度，M步更新混合系数、均值、方差，用于数据聚类                    |

### 考试易错点提醒：
1. MLE中对数似然的构造（乘积转求和，注意系数）。
2. EM算法中Jensen不等式的应用对象（凹函数$\ln x$）。
3. GMM的责任度计算（分子为当前分量的加权概率，分母为所有分量的加权概率和）。
4. EM算法的收敛性（局部极大值，依赖初始参数）。

# 统计模型期末考试计算题（EM算法与Jensen不等式）
**题目总分：20分**
**考察知识点**：Jensen不等式的应用、EM算法的E步与M步推导、高斯混合模型（GMM）的参数估计

## 题目背景
考虑一个单变量高斯混合模型（GMM），模型包含2个高斯分量，概率密度函数为：
$$p(x|\theta) = \alpha \cdot \mathcal{N}(x|\mu_1, \sigma_1^2) + (1-\alpha) \cdot \mathcal{N}(x|\mu_2, \sigma_2^2)$$
其中，模型参数 $\theta = (\alpha, \mu_1, \sigma_1^2, \mu_2, \sigma_2^2)$，满足 $0 < \alpha < 1$，$\sigma_1^2 > 0$，$\sigma_2^2 > 0$。

给定独立同分布（i.i.d.）的观测样本集 $X = \{x_1, x_2, ..., x_n\}$，引入隐变量 $z_i \in \{1, 2\}$（表示第 $i$ 个样本来自第 $z_i$ 个高斯分量），隐变量先验分布为：
$$p(z_i=1|\theta) = \alpha, \quad p(z_i=2|\theta) = 1-\alpha$$
完整数据似然为 $p(X, Z|\theta) = \prod_{i=1}^n p(x_i, z_i|\theta)$（$Z = \{z_1, z_2, ..., z_n\}$ 为隐变量集合）。

## 问题
### 1. Jensen不等式应用（5分）
证明：对于任意参数 $\theta$ 和 $\theta^{(t)}$（第 $t$ 次迭代的参数），有
$$\log p(X|\theta) \geq \mathcal{Q}(\theta, \theta^{(t)}) - \mathcal{H}(\theta, \theta^{(t)})$$
其中，
$$\mathcal{Q}(\theta, \theta^{(t)}) = \mathbb{E}_{Z|X, \theta^{(t)}}[\log p(X, Z|\theta)]$$
$$\mathcal{H}(\theta, \theta^{(t)}) = \mathbb{E}_{Z|X, \theta^{(t)}}[\log p(Z|X, \theta^{(t)})]$$
要求明确指出Jensen不等式的使用条件（函数凹凸性、分布归一性）。

### 2. EM算法的E步（7分）
第 $t$ 次迭代参数为 $\theta^{(t)} = (\alpha^{(t)}, \mu_1^{(t)}, (\sigma_1^2)^{(t)}, \mu_2^{(t)}, (\sigma_2^2)^{(t)})$：
（1）定义责任 $\gamma_{i1}^{(t)} = p(z_i=1|x_i, \theta^{(t)})$，$\gamma_{i2}^{(t)} = p(z_i=2|x_i, \theta^{(t)})$，推导二者表达式；
（2）基于（1）写出 $\mathcal{Q}(\theta, \theta^{(t)})$ 的具体表达式（展开对数并代入期望）。

### 3. EM算法的M步（8分）
最大化 $\mathcal{Q}(\theta, \theta^{(t)})$ 推导参数更新公式，要求写出求导和极值条件步骤：
（1）混合系数 $\alpha^{(t+1)}$；
（2）均值 $\mu_1^{(t+1)}$ 和 $\mu_2^{(t+1)}$；
（3）方差 $(\sigma_1^2)^{(t+1)}$ 和 $(\sigma_2^2)^{(t+1)}$。

## 参考答案与评分标准
### 1. Jensen不等式应用（5分）
**证明**：
1.  边缘似然变形：
    $$\log p(X|\theta) = \log \sum_Z p(X, Z|\theta) = \log \sum_Z p(Z|X, \theta^{(t)}) \cdot \frac{p(X, Z|\theta)}{p(Z|X, \theta^{(t)})}$$
    转化为期望形式：
    $$\log p(X|\theta) = \log \mathbb{E}_{Z|X, \theta^{(t)}} \left[ \frac{p(X, Z|\theta)}{p(Z|X, \theta^{(t)})} \right]$$

2.  Jensen不等式应用：
    - 对数函数 $\log(\cdot)$ 是**严格凹函数**（二阶导数<0）；
    - 凹函数的Jensen不等式：$\log \mathbb{E}[Y] \geq \mathbb{E}[\log Y]$（分布满足归一性 $\sum_Z p(Z|X, \theta^{(t)}) = 1$）；
    - 代入得：
      $$\log \mathbb{E}_{Z|X, \theta^{(t)}} \left[ \frac{p(X, Z|\theta)}{p(Z|X, \theta^{(t)})} \right] \geq \mathbb{E}_{Z|X, \theta^{(t)}} \left[ \log \frac{p(X, Z|\theta)}{p(Z|X, \theta^{(t)})} \right] = \mathcal{Q} - \mathcal{H}$$

**评分**：变形得2分，条件说明1分，不等式应用2分。

### 2. EM算法的E步（7分）
#### （1）责任推导（4分）
贝叶斯公式：
$$\gamma_{i1}^{(t)} = \frac{\alpha^{(t)} \cdot \mathcal{N}(x_i|\mu_1^{(t)}, (\sigma_1^2)^{(t)})}{\alpha^{(t)} \cdot \mathcal{N}(x_i|\mu_1^{(t)}, (\sigma_1^2)^{(t)}) + (1-\alpha^{(t)}) \cdot \mathcal{N}(x_i|\mu_2^{(t)}, (\sigma_2^2)^{(t)})}$$
$$\gamma_{i2}^{(t)} = 1 - \gamma_{i1}^{(t)}$$

**评分**：贝叶斯公式2分，分子分母表达式2分。

#### （2）$\mathcal{Q}$ 函数（3分）
完整数据对数似然：
$$\log p(X, Z|\theta) = \sum_{i=1}^n \left[ \log p(z_i|\theta) + \log p(x_i|z_i, \theta) \right]$$
期望展开：
$$\mathcal{Q}(\theta, \theta^{(t)}) = \sum_{i=1}^n \left[ \gamma_{i1}^{(t)} \left( \log \alpha + \log \mathcal{N}(x_i|\mu_1, \sigma_1^2) \right) + \gamma_{i2}^{(t)} \left( \log (1-\alpha) + \log \mathcal{N}(x_i|\mu_2, \sigma_2^2) \right) \right]$$

**评分**：完整似然1分，期望转化2分。

### 3. EM算法的M步（8分）
#### （1）混合系数 $\alpha^{(t+1)}$（2分）
提取相关项：$\mathcal{Q}_\alpha = \sum_{i=1}^n \left[ \gamma_{i1}^{(t)} \log \alpha + \gamma_{i2}^{(t)} \log (1-\alpha) \right]$
求导令为0：
$$\frac{\partial \mathcal{Q}_\alpha}{\partial \alpha} = \sum_{i=1}^n \left( \frac{\gamma_{i1}^{(t)}}{\alpha} - \frac{\gamma_{i2}^{(t)}}{1-\alpha} \right) = 0$$
解得：
$$\alpha^{(t+1)} = \frac{1}{n} \sum_{i=1}^n \gamma_{i1}^{(t)}$$

#### （2）均值更新（3分）
$\mu_1$ 相关项：$\mathcal{Q}_{\mu_1} = \sum_{i=1}^n \gamma_{i1}^{(t)} \left( -\frac{(x_i - \mu_1)^2}{2\sigma_1^2} \right)$
求导令为0：
$$\sum_{i=1}^n \gamma_{i1}^{(t)} (x_i - \mu_1) = 0 \implies \mu_1^{(t+1)} = \frac{\sum_{i=1}^n \gamma_{i1}^{(t)} x_i}{\sum_{i=1}^n \gamma_{i1}^{(t)}}$$
同理：
$$\mu_2^{(t+1)} = \frac{\sum_{i=1}^n \gamma_{i2}^{(t)} x_i}{\sum_{i=1}^n \gamma_{i2}^{(t)}}$$

#### （3）方差更新（3分）
$\sigma_1^2$ 相关项：$\mathcal{Q}_{\sigma_1^2} = \sum_{i=1}^n \gamma_{i1}^{(t)} \left( -\frac{1}{2} \log \sigma_1^2 - \frac{(x_i - \mu_1)^2}{2\sigma_1^2} \right)$
求导令为0（代入 $\mu_1^{(t+1)}$）：
$$\sum_{i=1}^n \gamma_{i1}^{(t)} \left( -\frac{1}{2\sigma_1^2} + \frac{(x_i - \mu_1^{(t+1)})^2}{2(\sigma_1^2)^2} \right) = 0$$
解得：
$$(\sigma_1^2)^{(t+1)} = \frac{\sum_{i=1}^n \gamma_{i1}^{(t)} (x_i - \mu_1^{(t+1)})^2}{\sum_{i=1}^n \gamma_{i1}^{(t)}}$$
同理：
$$(\sigma_2^2)^{(t+1)} = \frac{\sum_{i=1}^n \gamma_{i2}^{(t)} (x_i - \mu_2^{(t+1)})^2}{\sum_{i=1}^n \gamma_{i2}^{(t)}}$$

**评分**：$\alpha$ 更新2分，均值更新3分，方差更新3分（求导+极值条件为核心得分点）。