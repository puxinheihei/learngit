# 聚类分析核心知识点整理（期末复习版）
## 一、聚类分析基础概念
### 1. 聚类定义
聚类（又称无监督学习）是指在无先验标签的情况下，根据数据点间的相似度（或距离），将数据集划分为若干个**簇（Cluster）** 的过程。核心特征是：簇内数据点相似度高、簇间数据点相似度低，最终发现数据的自然分组结构。

### 2. 硬聚类vs软聚类
| 类型       | 定义                                  | 特点                                  | 适用场景                          |
|------------|---------------------------------------|---------------------------------------|-----------------------------------|
| 硬聚类     | 每个数据点仅属于一个簇                | 规则简单、易实现，应用更广泛          | 目标分组明确的场景（如客户分群）  |
| 软聚类     | 每个数据点可属于多个簇（带归属概率）  | 更灵活，符合实际中“边界数据”的特性    | 需多维度分类的场景（如商品标签分类）|

### 3. 距离度量（核心基础）
#### （1）距离的必备性质
- 对称性：$D(A,B)=D(B,A)$（A到B的距离等于B到A的距离）
- 自相似性：$D(A,A)=0$（自身距离为0）
- 分离性：$D(A,B)=0 \iff A=B$（距离为0当且仅当两点完全相同）
- 三角不等式：$D(A,B) \leq D(A,C)+D(B,C)$（两点距离不大于经第三点的路径距离）

#### （2）常用距离类型
- 欧氏距离（Euclidean Distance）：适用于连续型数据，计算两点在高维空间中的直线距离，是k-means的默认距离。
- 余弦相似度（Cosine Similarity）：适用于文本、向量类数据，衡量向量夹角的余弦值（值越近1越相似），常转化为距离使用。
- 杰卡德距离（Jaccard Distance）：适用于集合型数据（如文档关键词集合），基于交集与并集的比例计算相似度。
- 编辑距离（Edit Distance）：适用于字符串数据，衡量将一个字符串转化为另一个所需的最少插入、删除、替换操作次数（操作成本可自定义）。

### 4. 聚类的核心目标与挑战
- 核心目标：最大化簇内相似度（Intra-cluster Similarity），最小化簇间相似度（Inter-cluster Similarity）。
- 主要挑战：高维数据的“维度灾难”（多数点对距离相近）、噪声与离群点干扰、簇形状不规则、需手动指定参数（如k值）。

## 二、主流聚类方法（重点必考）
### 1. 划分方法：k-means算法（核心重点）
#### （1）定义
k-means是基于**质心（Centroid）** 的划分聚类算法，将数据划分为k个簇，每个簇由其质心（簇内所有点的均值向量）代表，通过迭代优化使簇内数据点到质心的距离之和最小。

#### （2）算法步骤
1. 输入参数：数据集$D$、簇数$k$（需预先指定）。
2. 初始化：随机选择k个数据点作为初始质心（或按“最远原则”选择，避免初始质心过近）。
3. 簇分配：对每个数据点，计算其到所有质心的距离，分配到距离最近的簇。
4. 质心更新：对每个簇，计算簇内所有数据点的均值向量，作为新的质心。
5. 迭代：重复步骤3和4，直到满足终止条件。
6. 输出：k个簇及对应的质心。

#### （3）终止条件
- 质心位置不再变化（或变化量小于阈值）。
- 数据点的簇归属不再改变。
- 达到预设的最大迭代次数。

#### （4）k值选择方法
通过“肘部法则”：尝试不同k值，计算每个k对应的“簇内平均距离到质心”，绘制k-距离曲线。曲线中“肘部”（距离下降速率骤减的点）对应的k即为最优值——此前距离快速下降（簇内相似度提升），此后下降平缓（增加k无明显收益）。

#### （5）优缺点（必考重点）
| 优点                                  | 缺点                                  |
|---------------------------------------|---------------------------------------|
| 效率高，时间复杂度为$O(tkn)$（t=迭代次数，k=簇数，n=数据点数），适用于大规模数据 | 需预先指定k值，对k的选择敏感          |
| 实现简单，逻辑直观                    | 仅适用于连续型数据（均值需有定义），无法处理分类数据 |
| 收敛速度快，通常迭代数较少            | 对噪声和离群点敏感（离群点会显著拉动质心） |
| 适合发现凸形簇（球形、椭圆形）        | 无法发现非凸形、不规则形状的簇        |
| 仅收敛到局部最优解（依赖初始质心）    | 对初始质心选择敏感，不同初始值可能得到不同结果 |

### 2. 层次方法（核心重点）
#### （1）定义
层次聚类通过构建**聚类树（Dendrogram）** 实现聚类，无需预先指定k值，而是通过“合并”或“分裂”簇，形成层次化的簇结构。

#### （2）分类
- 聚合式（AGNES，自底向上）：初始时每个数据点为一个簇，反复合并“最近”的两个簇，直到满足终止条件（如簇数为1或达到预设簇数）。
- 分裂式（DIANA，自顶向下）：初始时所有数据点为一个簇，反复分裂“最远”的簇，直到满足终止条件。

#### （3）类簇表示
- 质心（Centroid）：适用于欧氏空间，簇内所有点的均值向量（人工构造的虚拟点）。
- 聚类中心（Clustroid）：适用于非欧氏空间，簇内真实存在的、与其他点距离最近的数据点（如最小平均距离点、最小最大距离点）。

#### （4）类簇间距离定义（必考重点）
聚合式层次聚类的核心是定义“两个簇的距离”，常用4种方式：
1. 单链接（Single-link）：两个簇中**最近的两个数据点**的距离（“最相似点”决定簇距离）。
   - 公式：$D(C_1,C_2)=\min_{x \in C_1, y \in C_2} D(x,y)$
   - 特点：易形成“链状簇”，对噪声敏感，但能发现非凸簇。
2. 完全链接（Complete-link）：两个簇中**最远的两个数据点**的距离（“最不相似点”决定簇距离）。
   - 公式：$D(C_1,C_2)=\max_{x \in C_1, y \in C_2} D(x,y)$
   - 特点：簇结构更紧凑，避免链状簇，但对离群点敏感。
3. 平均链接（Average-link）：两个簇中**所有数据点对的平均距离**。
   - 公式：$D(C_1,C_2)=\frac{1}{|C_1| \cdot |C_2|} \sum_{x \in C_1} \sum_{y \in C_2} D(x,y)$
   - 特点：平衡单链接和完全链接的优缺点，应用广泛。
4. 质心链接（Centroid-link）：两个簇**质心之间的距离**。
   - 公式：$D(C_1,C_2)=D(\mu_1,\mu_2)$（$\mu_1$、$\mu_2$为两簇质心）
   - 特点：与k-means逻辑一致，但可能出现“逆合并”（合并后簇距离反而变小）。

#### （5）优缺点
| 优点                                  | 缺点                                  |
|---------------------------------------|---------------------------------------|
| 无需预先指定k值，可通过聚类树选择任意簇数 | 时间复杂度高（朴素实现$O(n^3)$，优化后$O(n^2\log n)$），不适合大规模数据 |
| 提供层次化簇结构，可解释性强          | 一旦合并/分裂簇，无法回溯调整          |
| 支持非欧氏距离，适用范围广            | 对噪声和离群点敏感（完全链接相对稳健） |
| 能发现非凸形簇（单链接）              | 高维数据中表现较差（受维度灾难影响）   |

### 3. 其他聚类方法（简要了解）
- 密度方法（DBSCAN）：基于“密度可达性”聚类，能发现任意形状簇、处理噪声，核心参数为$\epsilon$（邻域半径）和MinPts（邻域最小点数）。
- 模型方法（混合高斯模型）：将聚类视为概率模型估计问题，每个簇对应一个高斯分布，通过EM算法优化参数，支持软聚类。
- 网格方法：将数据空间划分为网格单元，基于单元密度聚类，适用于高维数据但精度较低。

## 三、聚类评价准则（必考重点）
聚类评价的核心是判断“聚类结果是否合理”，分为内部准则和外部准则（重点考外部准则）。

### 1. 内部准则
- 核心逻辑：无需真实标签，仅基于数据本身的距离/相似度衡量。
- 评价标准：簇内相似度越高（如簇内点到质心的平均距离越小）、簇间相似度越低（如簇质心间距离越大），聚类质量越好。
- 局限：无法验证聚类是否符合真实业务场景。

### 2. 外部准则（基于真实标签，必考）
需依赖“金标准”（Gold Standard）——数据的真实类别标签，衡量聚类结果与真实类别的一致性。

#### （1）纯度（Purity，核心重点）
- 定义：每个簇中“占比最高的真实类别”的样本数占该簇总样本数的比例，是衡量簇“单一性”的指标。
- 公式：
  - 单个簇纯度：$Purity(\omega_i)=\frac{1}{n_i} \max_j(n_{ij})$（$n_i$为簇$\omega_i$的大小，$n_{ij}$为簇$\omega_i$中属于真实类别$j$的样本数）
  - 整体纯度：$Purity=\frac{1}{n} \sum_{i=1}^k n_i \cdot Purity(\omega_i)$（$n$为总样本数，$k$为簇数）
- 特点：取值范围$[0,1]$，越接近1越好；但存在偏差——簇数越多，纯度越容易接近1（如每个样本为一个簇时纯度=1）。

#### （2）Rand指数（Rand Index, RI）
- 定义：衡量聚类结果与真实类别的“成对一致性”，统计所有样本对的四种情况：
  - A：同真实类别且同簇；
  - B：不同真实类别但同簇；
  - C：同真实类别但不同簇；
  - D：不同真实类别且不同簇。
- 公式：$RI=\frac{A+D}{A+B+C+D}$
- 特点：取值范围$[0,1]$，越接近1越好；无纯度的簇数偏差，但计算复杂度较高。

#### （3）纯度例题（必考）
##### 题目：
已知数据集包含3个真实类别（C1、C2、C3），聚类结果得到3个簇（$\omega_1$、$\omega_2$、$\omega_3$），各簇中真实类别的样本分布如下：
- $\omega_1$（6个样本）：C1=5，C2=1，C3=0
- $\omega_2$（6个样本）：C1=1，C2=4，C3=1
- $\omega_3$（5个样本）：C1=2，C2=0，C3=3
求整体纯度。

##### 解答：
1. 计算单个簇纯度：
   - $Purity(\omega_1)=\frac{1}{6} \max(5,1,0)=\frac{5}{6} \approx 0.833$
   - $Purity(\omega_2)=\frac{1}{6} \max(1,4,1)=\frac{4}{6} \approx 0.667$
   - $Purity(\omega_3)=\frac{1}{5} \max(2,0,3)=\frac{3}{5}=0.6$
2. 计算整体纯度（总样本数$n=6+6+5=17$）：
   - $Purity=\frac{1}{17} \times (6 \times \frac{5}{6} + 6 \times \frac{4}{6} + 5 \times \frac{3}{5}) = \frac{1}{17} \times (5+4+3) = \frac{12}{17} \approx 0.706$

## 四、计算题（必考重点）
### 1. k-means聚类计算题
#### 题目：
给定二维数据集$D=\{(1,2), (2,1), (4,5), (5,4), (3,3)\}$，使用k-means算法聚类（$k=2$），初始质心选择$C_1=(1,2)$、$C_2=(4,5)$，采用欧氏距离，迭代两次，求每次迭代后的簇分配结果和质心更新值。

#### 欧氏距离公式：
$D(x,y)=\sqrt{(x_1-y_1)^2 + (x_2-y_2)^2}$（比较距离大小时可省略根号，直接比较平方和）

#### 解答：
##### 初始状态：
- 簇1质心$C_1=(1,2)$，簇2质心$C_2=(4,5)$
- 所有样本未分配簇。

##### 第一次迭代：
1. 簇分配（计算每个样本到两质心的距离平方）：
   - 样本$(1,2)$：$D^2(C_1)=0$，$D^2(C_2)=(1-4)^2+(2-5)^2=9+9=18$ → 分配到簇1
   - 样本$(2,1)$：$D^2(C_1)=(2-1)^2+(1-2)^2=1+1=2$，$D^2(C_2)=(2-4)^2+(1-5)^2=4+16=20$ → 分配到簇1
   - 样本$(4,5)$：$D^2(C_1)=(4-1)^2+(5-2)^2=9+9=18$，$D^2(C_2)=0$ → 分配到簇2
   - 样本$(5,4)$：$D^2(C_1)=(5-1)^2+(4-2)^2=16+4=20$，$D^2(C_2)=(5-4)^2+(4-5)^2=1+1=2$ → 分配到簇2
   - 样本$(3,3)$：$D^2(C_1)=(3-1)^2+(3-2)^2=4+1=5$，$D^2(C_2)=(3-4)^2+(3-5)^2=1+4=5$ → 任选一个（此处分配到簇1）
2. 质心更新：
   - 簇1样本：$\{(1,2), (2,1), (3,3)\}$ → 新质心$C_1'=(\frac{1+2+3}{3}, \frac{2+1+3}{3})=(2, 2)$
   - 簇2样本：$\{(4,5), (5,4)\}$ → 新质心$C_2'=(\frac{4+5}{2}, \frac{5+4}{2})=(4.5, 4.5)$

##### 第二次迭代：
1. 簇分配（计算每个样本到$C_1'=(2,2)$、$C_2'=(4.5,4.5)$的距离平方）：
   - 样本$(1,2)$：$D^2(C_1')=(1-2)^2+(2-2)^2=1$，$D^2(C_2')=(1-4.5)^2+(2-4.5)^2=12.25+6.25=18.5$ → 簇1
   - 样本$(2,1)$：$D^2(C_1')=(2-2)^2+(1-2)^2=1$，$D^2(C_2')=(2-4.5)^2+(1-4.5)^2=6.25+12.25=18.5$ → 簇1
   - 样本$(4,5)$：$D^2(C_1')=(4-2)^2+(5-2)^2=4+9=13$，$D^2(C_2')=(4-4.5)^2+(5-4.5)^2=0.25+0.25=0.5$ → 簇2
   - 样本$(5,4)$：$D^2(C_1')=(5-2)^2+(4-2)^2=9+4=13$，$D^2(C_2')=(5-4.5)^2+(4-4.5)^2=0.25+0.25=0.5$ → 簇2
   - 样本$(3,3)$：$D^2(C_1')=(3-2)^2+(3-2)^2=2$，$D^2(C_2')=(3-4.5)^2+(3-4.5)^2=2.25+2.25=4.5$ → 簇1
2. 质心更新：
   - 簇1样本：$\{(1,2), (2,1), (3,3)\}$ → 新质心$C_1''=(2, 2)$（与上一轮一致）
   - 簇2样本：$\{(4,5), (5,4)\}$ → 新质心$C_2''=(4.5, 4.5)$（与上一轮一致）

##### 结论：
第二次迭代后质心不再变化，聚类收敛。最终簇分配：
- 簇1：$\{(1,2), (2,1), (3,3)\}$，质心$(2,2)$
- 簇2：$\{(4,5), (5,4)\}$，质心$(4.5,4.5)$

### 2. 层次聚类计算题
#### 题目：
给定4个数据点$A$、$B$、$C$、$D$，其成对欧氏距离矩阵如下（行/列均为$A,B,C,D$）：
|       | A    | B    | C    | D    |
|-------|------|------|------|------|
| A     | 0    | 3    | 6    | 7    |
| B     | 3    | 0    | 2    | 4    |
| C     | 6    | 2    | 0    | 5    |
| D     | 7    | 4    | 5    | 0    |
使用**聚合式层次聚类（完全链接）**，逐步合并簇，写出每一步的合并结果和簇间距离，并绘制简化聚类树。

#### 完全链接定义：
两簇距离=簇中所有样本对的最大距离。

#### 解答：
##### 初始状态：
- 簇集合：$\{A\}, \{B\}, \{C\}, \{D\}$（4个簇）
- 距离矩阵为输入矩阵。

##### 步骤1：寻找距离最小的簇对
- 所有簇对距离：$D(A,B)=3$，$D(A,C)=6$，$D(A,D)=7$，$D(B,C)=2$，$D(B,D)=4$，$D(C,D)=5$
- 最小距离=2（簇$\{B\}$和$\{C\}$）
- 合并$\{B\}$和$\{C\}$，形成新簇$\{B,C\}$
- 计算新簇$\{B,C\}$与其他簇的距离（完全链接，取最大距离）：
  - $D(\{B,C\}, A)=\max(D(B,A), D(C,A))=\max(3,6)=6$
  - $D(\{B,C\}, D)=\max(D(B,D), D(C,D))=\max(4,5)=5$

##### 步骤2：更新簇集合和距离矩阵
- 簇集合：$\{A\}, \{B,C\}, \{D\}$（3个簇）
- 新距离矩阵：
  |           | A    | {B,C} | D    |
  |-----------|------|-------|------|
  | A         | 0    | 6     | 7    |
  | {B,C}     | 6    | 0     | 5    |
  | D         | 7    | 5     | 0    |
- 最小距离=5（簇$\{B,C\}$和$\{D\}$）
- 合并$\{B,C\}$和$\{D\}$，形成新簇$\{B,C,D\}$
- 计算新簇$\{B,C,D\}$与$\{A\}$的距离（完全链接）：
  - $D(\{B,C,D\}, A)=\max(D(B,A), D(C,A), D(D,A))=\max(3,6,7)=7$

##### 步骤3：更新簇集合和距离矩阵
- 簇集合：$\{A\}, \{B,C,D\}$（2个簇）
- 新距离矩阵：
  |             | A    | {B,C,D} |
  |-------------|------|---------|
  | A           | 0    | 7       |
  | {B,C,D}     | 7    | 0       |
- 最小距离=7（簇$\{A\}$和$\{B,C,D\}$）
- 合并$\{A\}$和$\{B,C,D\}$，形成最终簇$\{A,B,C,D\}$（1个簇）

##### 聚类树（简化）：
        {A,B,C,D}
         /      \
        /        \
       7          \
      /            \
    {A}        {B,C,D}
                /    \
               /      \
              5        \
             /          \
          {B,C}        {D}
           /  \
          /    \
         2      \
        /        \
      {B}        {C}


## 五、核心知识点对比总结
| 对比维度       | k-means算法                          | 层次聚类（聚合式）                    |
|----------------|---------------------------------------|---------------------------------------|
| 核心思想       | 基于质心的迭代优化                   | 基于簇距离的层次合并/分裂             |
| 簇数k          | 需预先指定（肘部法则选择）            | 无需预先指定（聚类树中选择）          |
| 时间复杂度     | 低（$O(tkn)$），适用于大规模数据      | 高（$O(n^2\log n)$），适用于小规模数据 |
| 簇形状适应性   | 仅适用于凸形簇                        | 单链接支持非凸形簇                    |
| 对初始值敏感   | 是（依赖初始质心）                    | 否（依赖距离矩阵）                    |
| 噪声敏感性     | 敏感                                  | 完全链接相对稳健                      |
| 数据类型支持   | 仅连续型数据                          | 连续型、离散型（支持非欧氏距离）      |
| 可解释性       | 低（仅最终簇结果）                    | 高（层次化聚类树）                    |


### 如何评价聚类算法的好坏
聚类算法的评价需从**内部有效性**（基于数据自身特征）、**外部有效性**（基于先验知识/真实标签）和**实用性**三个维度展开，核心是衡量“簇内紧凑性、簇间分离性”，同时兼顾业务可解释性。

#### 1. 无标签场景（内部评价指标）
适用于无真实分类标签的场景，仅依赖数据分布特征：
| 指标名称 | 核心思想 | 计算逻辑 | 优缺点 |
|----------|----------|----------|--------|
| 轮廓系数（Silhouette Coefficient） | 衡量样本在簇内的紧密程度与簇间的分离程度 | 对每个样本，计算：<br>$s(i) = \frac{b(i)-a(i)}{\max(a(i),b(i))}$<br>（$a(i)$：样本i到同簇样本的平均距离；$b(i)$：样本i到最近异簇的平均距离）<br>整体轮廓系数为所有样本的平均值，范围[-1,1]，越接近1越好 | 优点：直观、无偏；<br>缺点：对球形簇友好，对非凸簇（如环形、不规则形状）评价偏差 |
| 戴维森堡丁指数（DBI） | 衡量簇内平均距离与簇间中心距离的比值 | $DBI = \frac{1}{k}\sum_{i=1}^k \max_{j≠i} \left( \frac{\text{avg}(C_i) + \text{avg}(C_j)}{d(\text{center}_i, \text{center}_j)} \right)$<br>（k为簇数，avg(C)为簇内样本平均距离，d为簇中心距离）<br>值越小，聚类效果越好 | 优点：对簇大小不敏感；<br>缺点：依赖簇中心的定义（如欧氏距离） |
| 卡林斯基-哈拉巴斯指数（CH Index） | 基于方差比，衡量簇间方差与簇内方差的比值 | $CH = \frac{\text{SSB}/(k-1)}{\text{SSW}/(n-k)}$<br>（SSB：簇间平方和；SSW：簇内平方和；n为样本数）<br>值越大，聚类效果越好 | 优点：计算高效；<br>缺点：倾向于划分出大小相近的球形簇 |
| 肘部法则（Elbow Method） | 基于簇内平方和（SSE）的变化趋势 | $SSE = \sum_{i=1}^k \sum_{x \in C_i} d(x, \text{center}_i)^2$<br>绘制k（簇数）-SSE曲线，曲线“肘部”对应的k为最优簇数 | 优点：简单易操作；<br>缺点：主观性强，无明确数值判断 |

#### 2. 有标签场景（外部评价指标）
适用于有真实分类标签的场景（如算法对比、验证），对比聚类结果与真实标签的一致性：
| 指标名称 | 核心思想 | 计算逻辑 | 优缺点 |
|----------|----------|----------|--------|
| 调整兰德指数（ARI） | 衡量聚类结果与真实标签的重合程度（修正随机一致性） | 基于混淆矩阵计算兰德系数，再修正随机概率，范围[-1,1]，越接近1越好 | 优点：无偏，适合不同簇数对比；<br>缺点：计算稍复杂 |
| 归一化互信息（NMI） | 衡量聚类结果与真实标签的互信息（归一化后） | $NMI = \frac{MI(Y, \hat{Y})}{\sqrt{H(Y)H(\hat{Y})}}$<br>（MI为互信息，H为熵），范围[0,1]，越接近1越好 | 优点：对簇数不敏感；<br>缺点：依赖熵的计算，对小簇不友好 |
| F值（F-measure） | 基于精确率（P）和召回率（R）的调和平均 | 对每个簇计算P和R，再求宏平均/微平均F值，范围[0,1]，越接近1越好 | 优点：贴合分类任务的评价逻辑；<br>缺点：需一对一匹配聚类簇与真实标签 |

#### 3. 实用性评价
- **计算效率**：是否适用于大规模数据（如K-Means高效，层次聚类低效）；
- **鲁棒性**：对噪声、异常值、数据尺度的敏感度（如DBSCAN抗噪声，K-Means易受异常值影响）；
- **业务可解释性**：聚类结果是否符合业务逻辑（如用户分群后，各簇的特征是否可解释）；
- **参数敏感性**：是否依赖大量调参（如K-Means需指定k，DBSCAN需调eps和min_samples）。

### 聚类算法的各种使用场景
聚类算法是无监督学习的核心，核心价值是“从无标签数据中发现潜在规律”，以下是典型场景及适配算法：

| 应用领域 | 具体场景 | 适配算法 | 核心原因 |
|----------|----------|----------|----------|
| 电商/用户运营 | 用户分群（如高价值、高活跃、流失风险用户） | K-Means、层次聚类、GMM | K-Means高效，适合大规模用户数据；<br>GMM适合用户特征呈高斯分布的场景；<br>层次聚类可输出分群层级（如“高价值-高活跃”→“高价值-低活跃”） |
| 金融 | 客户画像、欺诈检测（如异常交易聚类） | DBSCAN、LOF、K-Means | DBSCAN/LOF擅长发现异常点（欺诈交易）；<br>K-Means适合客户特征维度低、分布集中的场景 |
| 图像/计算机视觉 | 图像分割（如按像素特征聚类划分区域）、相似图片检索 | K-Means、谱聚类 | K-Means可对像素RGB/纹理特征聚类；<br>谱聚类适合非凸形状的图像区域分割 |
| 文本/NLP | 文本分类（如新闻聚类、评论情感分群）、关键词提取 | K-Means、LDA（主题聚类）、谱聚类 | K-Means适合文本向量化（TF-IDF/Word2Vec）后的快速聚类；<br>LDA可挖掘文本潜在主题，更贴合语义层面的聚类 |
| 工业/物联网 | 设备故障检测（如传感器数据异常聚类）、生产流程优化 | DBSCAN、K-Means、层次聚类 | DBSCAN对传感器噪声数据鲁棒，可识别故障设备的异常特征簇；<br>K-Means适合设备运行状态的常规分群 |
| 地理信息（GIS） | 区域划分（如城市商圈聚类、人口分布聚类） | DBSCAN、K-Means | DBSCAN可按地理坐标+人口密度聚类（非凸商圈）；<br>K-Means适合规则形状的区域划分（如行政区内的子区域） |
| 生物信息学 | 基因表达数据分析（如基因聚类、细胞分群） | 层次聚类、谱聚类、GMM | 层次聚类可展示基因/细胞的亲缘关系；<br>谱聚类适合基因表达数据的非凸分布；<br>GMM适合多模态的基因表达特征 |
| 推荐系统 | 物品聚类（如商品相似性聚类）、协同过滤补充 | K-Means、谱聚类 | K-Means将相似商品聚簇，简化推荐计算；<br>谱聚类适合商品特征复杂、非规则分布的场景 |
| 异常检测 | 网络攻击检测、服务器异常监控 | DBSCAN、OPTICS、LOF | DBSCAN/OPTICS无需指定簇数，可自动识别异常点；<br>LOF专注于局部异常值检测，适合服务器指标的突发异常 |

#### 场景选择核心原则
1. **数据规模**：大规模数据（百万级样本）优先选K-Means、Mini-Batch K-Means；小数据/需层级结果选层次聚类；
2. **数据分布**：凸形、球形簇选K-Means/GMM；非凸、不规则簇选DBSCAN/谱聚类；
3. **噪声/异常值**：含大量噪声选DBSCAN/LOF；数据干净选K-Means；
4. **业务需求**：需解释分群层级选层次聚类；需概率化结果选GMM；需无监督异常检测选DBSCAN/LOF。

### 总结
聚类算法的评价需“指标+业务”双维度结合（无标签看轮廓系数/DBI，有标签看ARI/NMI，最终落地看可解释性和效率）；场景选择需匹配数据特征（规模、分布、噪声）和业务目标（分群、异常检测、层级分析），无“最优算法”，只有“最适配算法”。