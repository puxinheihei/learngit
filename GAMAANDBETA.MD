# 统计模型核心函数与分布整理（期末复习版）
## 一、核心数学常数
### 1. 圆周率（π）
- **定义**：圆的周长与直径之比，是数学中最基本的常数之一。
- **数学表达式**：  
  $$\pi=\frac{圆的周长}{圆的直径}$$
- **近似值**：3.14159...

### 2. 自然常数（e）
- **定义**：满足极限 $\lim _{n \to \infty}\left(1+\frac{1}{n}\right)^{n}$ 的常数，是自然对数的底数。
- **近似值**：2.71828...

### 3. 黄金比例（φ）
- **定义**：方程 $x^2 - x - 1 = 0$ 的正实根。
- **数学表达式**：  
  $$\phi=\frac{1+\sqrt{5}}{2}$$
- **近似值**：1.618...

### 4. 欧拉公式相关常数
- **欧拉公式**：$e^{i \theta}=\cos \theta+i \sin \theta$  
  其中，$i$ 是虚数单位（$i^2=-1$），$\theta$ 为实变量（弧度制），$\cos\theta$、$\sin\theta$ 分别为余弦、正弦函数。
- **特殊情况**：当 $\theta=\pi$ 时，$e^{i\pi} + 1 = 0$（欧拉恒等式）。

## 二、Gamma函数
### 1. 定义
- 广义阶乘函数，是对自然数阶乘的延伸，适用于正实数及复数（此处仅讨论正实数）。
- **数学表达式**：  
  $$\Gamma(\alpha)=\int_{0}^{+\infty} x^{\alpha-1} e^{-x} d x \quad (\alpha>0)$$
- **另一种形式**（变量替换推导）：  
  $$\Gamma(\alpha)=2 \int_{0}^{+\infty} t^{2\alpha-1} e^{-t^2} d t$$

### 2. 核心性质
1. 递推公式：$\Gamma(\alpha+1)=\alpha \Gamma(\alpha)$（核心性质，连接相邻阶数的Gamma值）。
2. 自然数特例：对任意自然数 $n$，$\Gamma(n)=(n-1)!$（与阶乘直接挂钩）。
3. 特殊值：
   - $\Gamma(1)=1$（由 $\int_{0}^{+\infty} e^{-x} d x = 1$ 推导）。
   - $\Gamma\left(\frac{1}{2}\right)=\sqrt{\pi}$（重要常数，通过高斯积分推导）。
   - 半整数延伸：$\Gamma\left(n+\frac{1}{2}\right)=\frac{(2n-1)!!}{2^n} \sqrt{\pi}$（$!!$ 表示双阶乘，如 $3!!=3×1$，$5!!=5×3×1$）。

### 3. 计算例题
#### 题目：计算反常积分 $\int_{0}^{+\infty} \sqrt{x^5} e^{-x} d x$ 的值。
#### 解答步骤：
1. 整理被积函数，匹配Gamma函数定义形式：  
   $$\sqrt{x^5}=x^{\frac{5}{2}}=x^{\frac{7}{2}-1}$$  
   因此，原积分可改写为：  
   $$\int_{0}^{+\infty} x^{\frac{7}{2}-1} e^{-x} d x = \Gamma\left(\frac{7}{2}\right)$$

2. 利用递推公式逐步拆解：  
   $$\Gamma\left(\frac{7}{2}\right)=\Gamma\left(\frac{5}{2}+1\right)=\frac{5}{2} \Gamma\left(\frac{5}{2}\right)$$  
   $$\Gamma\left(\frac{5}{2}\right)=\Gamma\left(\frac{3}{2}+1\right)=\frac{3}{2} \Gamma\left(\frac{3}{2}\right)$$  
   $$\Gamma\left(\frac{3}{2}\right)=\Gamma\left(\frac{1}{2}+1\right)=\frac{1}{2} \Gamma\left(\frac{1}{2}\right)$$

3. 代入特殊值 $\Gamma\left(\frac{1}{2}\right)=\sqrt{\pi}$：  
   $$\Gamma\left(\frac{7}{2}\right)=\frac{5}{2}×\frac{3}{2}×\frac{1}{2}×\sqrt{\pi}=\frac{15}{8}\sqrt{\pi}$$

4. 最终结果：  
   $$\int_{0}^{+\infty} \sqrt{x^5} e^{-x} d x = \frac{15}{8}\sqrt{\pi} \approx 5.937$$

## 三、常用概率分布（含定义、公式、例题）
### 1. 二项分布（Binomial Distribution）
#### （1）定义与模型特征
- 离散型概率分布，描述 $n$ 次独立重复试验中“成功”次数的概率分布。
- 核心特征：
  - 二元结果：每次试验仅有“成功”或“失败”两种可能。
  - 成功概率恒定：每次试验的成功概率 $p$ 不变。
  - 试验独立：各次试验结果互不影响。

#### （2）核心公式
- 概率质量函数（PMF）：  
  $$P_B(R=r | n, p)=\binom{n}{r} p^r (1-p)^{n-r}$$  
  其中，$\binom{n}{r}=\frac{n!}{r!(n-r)!}$（组合数），$R$ 为成功次数随机变量，$n$ 为试验次数，$p$ 为单次成功概率。
- 均值：$E(R)=\mu=np$
- 方差：$Var(R)=\sigma^2=np(1-p)$（记 $q=1-p$，则方差为 $npq$）

#### （3）计算例题
##### 题目：
大批量生产的产品中，随机抽取20件检验。若样本中不合格品数≤3，则该批产品合格。已知生产过程的不合格品率平均为10%，求该批产品被接受的概率。
##### 解答步骤：
1. 确定参数：$n=20$（试验次数），$p=0.1$（单次“成功”=不合格品的概率），$r=0,1,2,3$（合格条件）。
2. 代入概率质量函数，计算累积概率：  
   $$P(接受)=F(x \leq 3)=\sum_{r=0}^{3} \binom{20}{r} (0.1)^r (0.9)^{20-r}$$
3. 分步计算各项：
   - $r=0$：$\binom{20}{0}(0.1)^0(0.9)^{20}≈0.1216$
   - $r=1$：$\binom{20}{1}(0.1)^1(0.9)^{19}≈0.2702$
   - $r=2$：$\binom{20}{2}(0.1)^2(0.9)^{18}≈0.2852$
   - $r=3$：$\binom{20}{3}(0.1)^3(0.9)^{17}≈0.1890$
4. 求和得结果：  
   $$P(接受)≈0.1216+0.2702+0.2852+0.1890=0.866$$

### 2. 泊松分布（Poisson Distribution）
#### （1）定义与模型特征
- 离散型概率分布，描述单位时间/空间内“稀有事件”发生次数的概率分布。
- 核心特征：
  - 事件发生概率小（稀有事件）。
  - 事件独立：各事件发生互不影响。
  - 发生速率恒定：单位时间/空间内平均发生次数固定。

#### （2）核心公式
- 概率质量函数（PMF）：  
  $$P_P(X=k | \lambda)=\frac{e^{-\lambda} \lambda^k}{k!}$$  
  其中，$X$ 为事件发生次数随机变量，$\lambda$ 为单位时间/空间内平均发生次数（$\lambda=np$，可作为二项分布的近似，当 $p→0$ 且 $n→∞$ 时）。
- 均值：$E(X)=\mu=\lambda$
- 方差：$Var(X)=\sigma^2=\lambda$（均值=方差是泊松分布的重要特征）

#### （3）计算例题
##### 题目：
某种血清注射后产生不良反应的概率为0.001。求1000人注射后，至少2人出现不良反应的概率。
##### 解答步骤：
1. 确定参数：$\lambda=np=1000×0.001=1$（平均不良反应人数）。
2. 转化问题：“至少2人”=1 - “0人或1人出现不良反应”。
3. 计算累积概率：  
   $$P(X≥2)=1 - P(X≤1)=1 - \left[\frac{e^{-1}×1^0}{0!} + \frac{e^{-1}×1^1}{1!}\right]$$
4. 代入计算：  
   $$\frac{e^{-1}×1^0}{0!}=e^{-1}≈0.3679，\frac{e^{-1}×1^1}{1!}=e^{-1}≈0.3679$$  
   $$P(X≥2)=1 - (0.3679+0.3679)=0.2642$$

### 3. 指数分布（Exponential Distribution）
#### （1）定义与模型特征
- 连续型概率分布，描述“泊松过程中相邻两次事件发生的时间间隔”的概率分布。
- 核心特征：
  - 无记忆性：$P(T>s+t | T>s)=P(T>t)$（未来间隔与已发生间隔无关）。
  - 仅依赖泊松过程的平均发生速率 $\lambda$。

#### （2）核心公式
- 概率密度函数（PDF）：  
  $$f_E(T=t | \lambda)=\lambda e^{-\lambda t} \quad (t≥0)$$  
  其中，$T$ 为时间间隔随机变量，$\lambda$ 为泊松过程的平均发生速率（$\lambda=1/\mu$，$\mu$ 为平均间隔时间）。
- 累积分布函数（CDF）：  
  $$F(t≤k)=1 - e^{-\lambda k}$$
- 均值：$E(T)=\mu=1/\lambda$
- 方差：$Var(T)=\sigma^2=1/\lambda^2$

#### （3）计算例题
##### 题目：
某品牌灯泡的寿命服从指数分布，平均寿命为1000小时。求该灯泡寿命超过1000小时的概率。
##### 解答步骤：
1. 确定参数：$\mu=1000$ 小时，故 $\lambda=1/\mu=0.001$。
2. 转化问题：“寿命超过1000小时”=1 - “寿命≤1000小时”。
3. 代入累积分布函数：  
   $$P(T>1000)=1 - F(t≤1000)=1 - (1 - e^{-0.001×1000})$$
4. 计算结果：  
   $$P(T>1000)=e^{-1}≈0.368$$

### 4. 正态分布（Normal/Gaussian Distribution）
#### （1）定义与模型特征
- 连续型概率分布，描述“大量独立随机变量之和”的极限分布，呈“钟形曲线”。
- 核心特征：
  - 对称性：曲线关于均值 $\mu$ 对称。
  - 参数决定形状：$\mu$（均值）决定曲线位置，$\sigma$（标准差）决定曲线陡峭程度（$\sigma$ 越小越陡峭）。
  - 广泛适用性：可近似多种分布（如二项分布、泊松分布）。

#### （2）核心公式
- 概率密度函数（PDF）：  
  $$f_N(x | \mu, \sigma^2)=\frac{1}{\sqrt{2\pi\sigma^2}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}$$  
  其中，$x$ 为随机变量取值，$\mu$ 为均值，$\sigma^2$ 为方差（$\sigma>0$）。
- 标准正态分布：当 $\mu=0$、$\sigma=1$ 时，记为 $Z\sim N(0,1)$，其PDF为：  
  $$\phi(z)=\frac{1}{\sqrt{2\pi}} e^{-\frac{z^2}{2}}$$
- 标准化变换：对任意正态变量 $X\sim N(\mu,\sigma^2)$，可通过 $Z=\frac{x-\mu}{\sigma}$ 转化为标准正态变量。

#### （3）计算例题
##### 题目：
某公司员工年龄服从正态分布 $N(50, 5^2)$（均值50岁，标准差5岁）。求年龄在50~52.5岁之间的员工占比。
##### 解答步骤：
1. 确定参数：$\mu=50$，$\sigma=5$，目标区间 $50<x≤52.5$。
2. 标准化变换：
   - 当 $x=50$ 时，$Z_1=\frac{50-50}{5}=0$。
   - 当 $x=52.5$ 时，$Z_2=\frac{52.5-50}{5}=0.5$。
3. 查标准正态分布表（或软件）得累积概率：
   - $F(Z≤0)=0.5$（标准正态分布的对称性）。
   - $F(Z≤0.5)=0.6915$。
4. 计算区间概率：  
   $$P(50<x≤52.5)=F(Z≤0.5)-F(Z≤0)=0.6915-0.5=0.1915$$
5. 转化为占比：19.15%

### 5. Beta分布（Beta Distribution）
#### （1）定义与模型特征
- 连续型概率分布，取值范围被限制在 $[0,1]$ 之间，适用于描述“比例类变量”的概率分布。
- 核心特征：
  - 有界性：随机变量 $x\in[0,1]$（如比例、百分比）。
  - 形状灵活：由两个形状参数 $\lambda_1$（α）、$\lambda_2$（β）控制分布形状。
  - 与Gamma函数关联：概率密度函数通过Gamma函数定义。

#### （2）核心公式
- 常用概率密度函数（PDF）：  
  $$f(x | \lambda_1, \lambda_2)=\frac{\Gamma(\lambda_1+\lambda_2)}{\Gamma(\lambda_1)\Gamma(\lambda_2)} x^{\lambda_1-1} (1-x)^{\lambda_2-1}$$  
  其中，$\lambda_1,\lambda_2≥0$，$0≤x≤1$。
- 整数参数特例（$\lambda_1,\lambda_2$ 为正整数）：  
  $$f(x | \lambda_1, \lambda_2)=\frac{(\lambda_1+\lambda_2-1)!}{(\lambda_1-1)!(\lambda_2-1)!} x^{\lambda_1-1} (1-x)^{\lambda_2-1}$$
- 均值：$E(X)=\mu=\frac{\lambda_1}{\lambda_1+\lambda_2}$（记 $n=\lambda_1+\lambda_2$，$r=\lambda_1$，则 $\mu=r/n$）。
- 方差：$Var(X)=\sigma^2=\frac{\lambda_1\lambda_2}{(\lambda_1+\lambda_2)^2(\lambda_1+\lambda_2+1)}$

#### （3）计算例题
##### 题目：
某公司员工提交医疗福利申请的比例服从Beta分布。已知40名员工中，预计30%会提交申请（即均值=0.3），求超过16名员工提交申请的概率。
##### 解答步骤：
1. 确定参数：
   - 总员工数 $n=40$（对应 $\lambda_1+\lambda_2=40$）。
   - 均值 $\mu=r/n=0.3$，故 $r=\lambda_1=0.3×40=12$，$\lambda_2=40-12=28$。
   - 目标比例：$x=16/40=0.4$（超过16人即 $x>0.4$）。
2. 转化问题：$P(X>0.4)=1 - P(X≤0.4)$。
3. 利用Beta分布累积分布函数（不完全Beta函数）：  
   $$F(X≤0.4 | \lambda_1=12, \lambda_2=28)=\frac{(40-1)!}{(12-1)!(28-1)!} \int_{0}^{0.4} v^{11} (1-v)^{27} d v$$
4. 查表或软件计算累积概率：$F(X≤0.4)=0.91$。
5. 计算结果：  
   $$P(X>0.4)=1 - 0.91=0.09$$

## 四、核心知识点关联总结
| 函数/分布       | 核心用途                  | 关键关联                  |
|-----------------|---------------------------|---------------------------|
| Gamma函数       | 广义阶乘、支撑其他分布    | 是Beta分布、Dirichlet分布的基础 |
| 二项分布        | 离散型成功次数建模        | 可由泊松分布近似（$p→0$） |
| 泊松分布        | 稀有事件发生次数建模      | 与指数分布为“计数-间隔”对偶 |
| 指数分布        | 时间间隔建模              | 具有无记忆性              |
| 正态分布        | 连续型变量建模（广泛适用）| 可近似多种分布            |
| Beta分布        | 比例类变量建模（$[0,1]$） | 与Gamma函数联合定义        |