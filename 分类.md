# 分类算法核心知识点整理（期末复习版）
## 一、分类算法基础概念
### 1. 分类定义
分类是**监督学习**的核心任务，指给定带标签的训练样本集，学习一个映射规则（分类器），将未知类别的新样本分配到离散的类别集合中（如“垃圾邮件/正常邮件”“好瓜/坏瓜”）。

### 2. 核心逻辑框架
训练样本（带标签）→ 学习分类模型 / 规则 → 测试样本（无标签）→ 输出类别标签
- 训练样本：已知输入特征和对应类别标签的数据（如标注“spam”的邮件）。
- 测试样本：仅知输入特征，需通过分类器预测类别的数据。
- 分类器：通过训练过程得到的映射规则（如决策树、贝叶斯模型、SVM等）。

### 3. 数据划分（避免过拟合）
- 训练集：用于学习模型参数（占比60%-70%）。
- 验证集：用于调优超参数（如决策树的剪枝参数、KNN的k值），避免过拟合（占比10%-20%）。
- 测试集：用于评估模型最终性能，需独立于训练/验证集（占比10%-20%）。

### 4. 分类类型
- 二分类：类别集合仅含2类（如“是/否”“fraud/无fraud”）。
- 多分类：类别集合含3类及以上（如“数字0-9识别”“疾病类型划分”）。
- 多标签分类：单个样本可属于多个类别（如“新闻标签：体育+篮球”）。

## 二、典型分类场景（必考简答题）
| 场景名称               | 输入特征                          | 输出类别                  | 核心应用价值                  |
|------------------------|-----------------------------------|---------------------------|-------------------------------|
| 垃圾邮件过滤           | 邮件文本（关键词、发送者、格式）  | 垃圾邮件/正常邮件（ham）  | 过滤无关信息，提升沟通效率    |
| 数字识别               | 图像像素矩阵、形状特征（如笔画数）| 数字0-9                   | 手写输入识别、验证码自动识别  |
| 欺诈检测               | 账户交易记录（金额、频率、地点）  | 欺诈交易/正常交易          | 降低金融风险，保护用户资产    |
| 医疗诊断               | 患者症状、检查指标（如血压、血常规）| 患病/未患病、疾病类型      | 辅助医生诊断，提高诊断效率    |
| 网页垃圾检测           | HTML内容、链接结构、关键词        | 垃圾网页/正常网页          | 优化搜索引擎结果，提升用户体验|
| 客户服务邮件路由       | 邮件文本、主题、发送者身份        | 对应客服部门（售后/售前）  | 提高邮件处理效率，提升客户满意度|
| 语音识别               | 音频波形特征                      | 语音对应的文字/phonemes    | 智能语音助手（如Siri）、实时翻译|
## 分类算法核心使用场景整理（基于课程PDF）
| 分类算法       | 核心适用场景                                                                 | 场景说明与PDF关联示例                                                                 |
|----------------|------------------------------------------------------------------------------|--------------------------------------------------------------------------------------|
| 朴素贝叶斯     | 1. 文本分类（垃圾邮件/正常邮件、网页垃圾检测）；2. 短文本快速分类（客户服务邮件路由）；3. 高维数据分类（蛋白质序列识别） | - PDF明确以“垃圾邮件过滤”为典型示例，利用邮件关键词、发送者等特征快速判断类别；<br>- 适合特征独立假设的高维数据，如文本、序列类数据（PDF提及蛋白质序列、Unix系统调用序列等数据类型）。 |
| 决策树（ID3/C4.5/CART） | 1. 结构化数据分类（如“好瓜/坏瓜”判断、客户分群）；2. 医疗诊断（症状→疾病分类）；3. 工业质量检测（产品特征→合格/不合格）；4. 连续/离散混合属性数据分类 | - PDF以“好瓜分类”为核心示例，通过色泽、根蒂等离散特征构建规则树；<br>- C4.5支持连续属性（如PDF中温度、湿度等连续特征处理），适用于多属性决策场景；<br>- 规则可解释性强，适合需要明确分类逻辑的场景（如医疗诊断、质量检测）。 |
| SVM（支持向量机） | 1. 小样本、高维数据分类（数字识别、文本分类）；2. 图像识别（像素特征→类别）；3. 非线性分类任务（核函数映射高维空间） | - PDF提及“数字识别”为state-of-the-art应用，通过像素网格、形状特征实现高精度分类；<br>- 适合数据量少但特征维度高的场景，如语音波形特征分类、文本核函数映射分类。 |
| KNN（K近邻）   | 1. 模式识别（如手写数字识别、图像匹配）；2. 欺诈检测（账户交易特征→欺诈/正常）；3. 推荐系统中的相似分类（用户行为→偏好类别）；4. 实时性要求不高的小数据集分类 | - 基于“近朱者赤”逻辑，无需训练模型，适合快速验证分类效果；<br>- 适配PDF中“向量型数据”“图像数据”等类型，如通过欧氏距离匹配相似样本类别。 |
| 逻辑回归       | 1. 二分类任务（欺诈检测、疾病诊断→患病/未患病）；2. 风险评估（金融交易→风险等级）；3. 线性可分数据快速分类 | - 模型简单、可解释性强，适合需要输出概率的二分类场景（如PDF提及的“欺诈/无欺诈”检测）；<br>- 适配结构化数据（如账户交易金额、频率等特征），训练速度快，适合大规模数据初步分类。 |
| 神经网络/深度学习 | 1. 图像识别（数字识别、图像分割）；2. 语音识别（音频波形→文字/ phonemes）；3. 复杂序列数据分类（Unix系统调用序列、蛋白质序列）；4. 高精度分类任务（自动作文评分、医疗影像诊断） | - PDF以“数字识别”为示例，说明其达到“人类水平性能”；<br>- 适合处理非结构化数据（图像、音频、长文本），适配PDF中提及的图像像素网格、语音波形等数据类型。 |
| 集成学习（随机森林/Boosting） | 1. 高准确率要求场景（医疗诊断、金融风险评估）；2. 噪声数据分类（如含异常值的交易数据、工业检测数据）；3. 复杂决策场景（多特征融合分类） | - 结合多个基模型优势，降低过拟合风险；<br>- 适配PDF中“欺诈检测”“医疗诊断”等对准确率要求高的场景，可处理含噪声的结构化数据（如账户交易异常值、患者症状冗余信息）。 |
| 逻辑回归（核化版） | 1. 非线性二分类任务（如网页分类、用户行为预测）；2. 高维稀疏数据分类（文本特征→类别） | - PDF提及“核化逻辑回归”为先进分类器，适合文本等稀疏高维数据，通过核函数处理非线性关系。 |

## 三、常用分类方法概念（核心名词）
### 1. 朴素贝叶斯（Naive Bayes）
- **定义**：基于贝叶斯定理和“特征条件独立假设”的分类算法。
- **核心原理**：通过计算待分类样本属于每个类别的后验概率，选择概率最大的类别作为预测结果。
- **公式核心**：$P(C_k|X)=\frac{P(X|C_k)P(C_k)}{P(X)}$，其中$X$为样本特征，$C_k$为第$k$类。
- **特点**：假设特征间相互独立，计算简单、速度快，适用于高维数据（如文本分类）。

### 2. 决策树（Decision Tree）
- **定义**：以树状结构表示分类规则的算法，通过递归划分特征空间，将样本分配到对应的类别。
- **核心结构**：
  - 内部节点：表示一个特征（如“纹理=清晰？”“根蒂=蜷缩？”）。
  - 叶节点：表示一个类别（如“好瓜”“坏瓜”）。
  - 边：表示特征的取值（如“清晰”“稍糊”“模糊”）。
- **核心思想**：每次选择“最优特征”划分样本，使划分后的数据“纯度提升最大”（簇内类别越单一越好）。

### 3. 支持向量机（SVM）
- **定义**：通过寻找“最优超平面”实现分类的算法，最优超平面是指两类样本间隔最大的超平面。
- **核心原理**：在低维空间无法线性划分时，通过核函数映射到高维空间，再寻找线性超平面。
- **特点**：泛化能力强，适合小样本、高维数据，对噪声不敏感。

### 4. K近邻（K-Nearest Neighbor, KNN）
- **定义**：基于“近朱者赤”的惰性学习算法，无需训练模型，直接通过测试样本的$k$个最近邻样本的类别投票预测其类别。
- **核心要素**：
  - 距离度量：常用欧氏距离、曼哈顿距离。
  - $k$值：近邻样本的数量（$k$过小易过拟合，$k$过大易欠拟合）。
  - 投票规则：多数表决（$k$个近邻中占比最高的类别为预测结果）。

### 5. 逻辑回归（Logistic Regression）
- **定义**：虽名为“回归”，实为二分类算法，通过Sigmoid函数将线性回归的输出映射到$[0,1]$区间，作为样本属于某类的概率。
- **核心公式**：$P(Y=1|X)=\frac{1}{1+e^{-(w\cdot X+b)}}$，其中$w$为权重，$b$为偏置。
- **特点**：模型简单、可解释性强，适合处理线性可分数据。

## 四、决策树重点模块（必考简答题+计算题）
### 1. 决策树的核心：分类属性选择（四大指标）
分类属性选择的目标是**最大化划分后的数据纯度**，常用四大指标：

#### （1）信息熵（Entropy）
- **定义**：衡量样本集合纯度的指标，熵值越小，样本类别越集中（纯度越高）。
- **公式**：对于含$K$个类别的样本集合$D$，第$k$类样本占比为$p_k$（$\sum_{k=1}^K p_k=1$），则：  
  $$Ent(D)=-\sum_{k=1}^K p_k \log_2 p_k$$
- **取值范围**：$Ent(D) \in [0, \log_2 K]$。
  - 当所有样本属于同一类别时，$Ent(D)=0$（纯度最高）。
  - 当样本均匀分布在所有类别时，$Ent(D)$最大（纯度最低）。

#### （2）信息增益（Information Gain）
- **定义**：引入某特征$a$划分样本集$D$后，信息熵的减少量，增益越大，说明该特征划分纯度提升越明显。
- **公式**：设特征$a$有$V$个取值，划分后得到$V$个子集$D^1,D^2,...,D^V$，则：  
  $$Gain(D,a)=Ent(D)-\sum_{v=1}^V \frac{|D^v|}{|D|} Ent(D^v)$$
  - $\frac{|D^v|}{|D|}$：子集$D^v$占总样本集$D$的比例。

#### （3）信息增益率（Gain Ratio）
- **定义**：为解决信息增益偏向“取值多的特征”的问题，引入“固有值$IV(a)$”对信息增益进行归一化。
- **公式**：  
  $$Gain\_ratio(D,a)=\frac{Gain(D,a)}{IV(a)}$$  
  其中，固有值$IV(a)=-\sum_{v=1}^V \frac{|D^v|}{|D|} \log_2 \frac{|D^v|}{|D|}$（特征$a$的取值越分散，$IV(a)$越大）。

#### （4）Gini系数（Gini Index）
- **定义**：衡量样本集合的“不纯度”，Gini值越小，样本类别越集中（纯度越高），适用于CART算法。
- **公式**：  
  $$Gini(D)=1-\sum_{k=1}^K p_k^2$$
  - 物理含义：从样本集$D$中随机抽取两个样本，类别不同的概率。

### 2. ID3算法（重点简答题）
#### （1）定义
ID3（Iterative Dichotomiser 3）是基于**信息增益**选择分类属性的决策树构建算法，核心是每次选择信息增益最大的特征划分样本，递归构建树直到所有子集纯度为1（或无特征可选）。

#### （2）核心步骤
1. 输入：训练样本集$D$、特征集合$A$。
2. 若$D$中所有样本属于同一类别，返回该类别作为叶节点。
3. 若$A$为空（无特征可选），返回$D$中占比最高的类别作为叶节点。
4. 计算$A$中每个特征的信息增益$Gain(D,a)$。
5. 选择信息增益最大的特征$a^*$作为当前内部节点。
6. 对$a^*$的每个取值$v$，生成子集$D^v$，递归调用步骤2-5，构建子树。
7. 输出决策树。

#### （3）优缺点
| 优点                                  | 缺点                                  |
|---------------------------------------|---------------------------------------|
| 原理简单、易实现，训练速度快          | 偏向选择取值多的特征（如“身份证号”这类无意义高取值特征） |
| 决策树结构清晰，可解释性强            | 无法处理连续型特征（仅支持离散型特征） |
| 无需预处理特征（如归一化）            | 易过拟合（无剪枝机制，对噪声敏感）    |

### 3. C4.5算法（重点简答题）
#### （1）定义
C4.5是ID3算法的改进版，核心优化是采用**信息增益率**选择分类属性，解决了ID3的固有缺陷，是更实用的决策树算法。

#### （2）核心改进点（对比ID3）
1. 用**信息增益率**替代信息增益选择特征，避免偏向高取值特征。
2. 支持**连续型特征**：将连续特征离散化（如温度“60-80℃”划分为“中温”），计算每个划分点的信息增益率。
3. 引入**剪枝机制**：通过修剪冗余分支，降低过拟合风险。
4. 处理**缺失值**：通过样本权重分配，对缺失特征的样本进行合理划分。

#### （3）核心步骤
1. 输入：训练样本集$D$、特征集合$A$（含离散/连续特征）。
2. 若$D$中所有样本属于同一类别，返回该类别作为叶节点。
3. 若$A$为空，返回$D$中占比最高的类别作为叶节点。
4. 对连续特征离散化，计算所有特征（离散+离散化后的连续特征）的信息增益率$Gain\_ratio(D,a)$。
5. 选择信息增益率最大的特征$a^*$作为当前内部节点。
6. 对$a^*$的每个取值（离散特征）或划分区间（连续特征），生成子集$D^v$，递归构建子树。
7. 剪枝：移除泛化能力差的分支（如样本数过少的叶节点）。
8. 输出决策树。

#### （4）优缺点
| 优点                                  | 缺点                                  |
|---------------------------------------|---------------------------------------|
| 解决ID3的高取值特征偏向问题            | 计算复杂度高于ID3（需计算信息增益率和离散化） |
| 支持连续型特征和缺失值，适用性更广    | 剪枝过程需调参，逻辑较复杂            |
| 抗过拟合能力更强（剪枝机制）          | 仍不适用于大规模数据（训练时间较长）  |
| 决策树可解释性强                      | 对噪声数据的鲁棒性一般                |

## 五、计算题（必考重点）
### 1. 决策树计算题：信息增益计算（基于好瓜数据集）
#### 题目：
给定好瓜训练数据集（共17个样本），类别为“好瓜（8个）”和“坏瓜（9个）”。特征“色泽”的取值为{青绿（6个）、乌黑（6个）、浅白（5个）}，其中：
- 青绿子集$D^1$：好瓜3个，坏瓜3个；
- 乌黑子集$D^2$：好瓜4个，坏瓜2个；
- 浅白子集$D^3$：好瓜1个，坏瓜4个。
计算特征“色泽”的信息增益$Gain(D, 色泽)$。

#### 解答步骤：
##### 步骤1：计算根节点的信息熵$Ent(D)$
总样本数$|D|=17$，好瓜占比$p_1=\frac{8}{17}$，坏瓜占比$p_2=\frac{9}{17}$：  
$$Ent(D)=-\left(p_1 \log_2 p_1 + p_2 \log_2 p_2\right)=-\left(\frac{8}{17}\log_2 \frac{8}{17} + \frac{9}{17}\log_2 \frac{9}{17}\right) \approx 0.998$$

##### 步骤2：计算每个子集的信息熵$Ent(D^v)$
- 子集$D^1$（青绿，$|D^1|=6$）：好瓜3个，坏瓜3个  
  $$Ent(D^1)=-\left(\frac{3}{6}\log_2 \frac{3}{6} + \frac{3}{6}\log_2 \frac{3}{6}\right)=1.000$$
- 子集$D^2$（乌黑，$|D^2|=6$）：好瓜4个，坏瓜2个  
  $$Ent(D^2)=-\left(\frac{4}{6}\log_2 \frac{4}{6} + \frac{2}{6}\log_2 \frac{2}{6}\right) \approx 0.918$$
- 子集$D^3$（浅白，$|D^3|=5$）：好瓜1个，坏瓜4个  
  $$Ent(D^3)=-\left(\frac{1}{5}\log_2 \frac{1}{5} + \frac{4}{5}\log_2 \frac{4}{5}\right) \approx 0.722$$

##### 步骤3：计算信息增益$Gain(D, 色泽)$
$$Gain(D, 色泽)=Ent(D)-\left(\frac{|D^1|}{|D|}Ent(D^1) + \frac{|D^2|}{|D|}Ent(D^2) + \frac{|D^3|}{|D|}Ent(D^3)\right)$$
代入数值：  
$$Gain(D, 色泽)=0.998 - \left(\frac{6}{17}×1.000 + \frac{6}{17}×0.918 + \frac{5}{17}×0.722\right) \approx 0.109$$

#### 结论：
特征“色泽”的信息增益约为0.109，若其他特征的信息增益更小，则“色泽”会被选为当前划分特征。

### 2. K近邻计算题：欧氏距离分类
#### 题目：
给定二维训练数据集（类别标签为“+”或“-”）：
- 正例（+）：$A(1,2)$、$B(2,3)$、$C(3,1)$、$D(4,2)$
- 负例（-）：$E(2,1)$、$F(3,3)$、$G(4,1)$、$H(5,3)$
测试样本$X(3,2)$，采用欧氏距离和多数表决规则，取$k=3$，预测$X$的类别。

#### 欧氏距离公式：
$$D(x,y)=\sqrt{(x_1-y_1)^2 + (x_2-y_2)^2}$$

#### 解答步骤：
##### 步骤1：计算测试样本$X$与所有训练样本的欧氏距离
- $D(X,A)=\sqrt{(3-1)^2 + (2-2)^2}=\sqrt{4}=2.0$
- $D(X,B)=\sqrt{(3-2)^2 + (2-3)^2}=\sqrt{2}≈1.414$
- $D(X,C)=\sqrt{(3-3)^2 + (2-1)^2}=\sqrt{1}=1.0$
- $D(X,D)=\sqrt{(3-4)^2 + (2-2)^2}=\sqrt{1}=1.0$
- $D(X,E)=\sqrt{(3-2)^2 + (2-1)^2}=\sqrt{2}≈1.414$
- $D(X,F)=\sqrt{(3-3)^2 + (2-3)^2}=\sqrt{1}=1.0$
- $D(X,G)=\sqrt{(3-4)^2 + (2-1)^2}=\sqrt{2}≈1.414$
- $D(X,H)=\sqrt{(3-5)^2 + (2-3)^2}=\sqrt{5}≈2.236$

##### 步骤2：选择距离最小的$k=3$个近邻
按距离从小到大排序：  
$C(1.0)$、$D(1.0)$、$F(1.0)$（距离均为1.0，并列前3）

##### 步骤3：多数表决确定类别
- 近邻$C$：正例（+）
- 近邻$D$：正例（+）
- 近邻$F$：负例（-）
正例占2票，负例占1票，多数表决结果为**正例（+）**。

#### 结论：
测试样本$X(3,2)$的预测类别为正例（+）。

## 六、核心知识点对比总结
### 1. ID3与C4.5算法对比
| 对比维度       | ID3算法                              | C4.5算法                              |
|----------------|---------------------------------------|---------------------------------------|
| 特征选择指标   | 信息增益                              | 信息增益率                            |
| 连续型特征支持 | 不支持                                | 支持（离散化处理）                    |
| 缺失值处理     | 不支持                                | 支持（权重分配）                      |
| 过拟合处理     | 无剪枝机制，易过拟合                  | 有剪枝机制，抗过拟合能力强            |
| 计算复杂度     | 低（仅计算信息增益）                  | 高（计算增益率+离散化+剪枝）          |
| 特征偏向       | 偏向取值多的特征                      | 无明显特征偏向                        |

### 2. 分类属性选择指标对比
| 指标名称       | 核心用途                              | 优缺点                                  |
|----------------|---------------------------------------|---------------------------------------|
| 信息熵         | 衡量样本集纯度                        | 直观易懂，但仅能描述纯度，无法直接用于特征选择 |
| 信息增益       | ID3算法的特征选择依据                  | 计算简单，纯度提升直观；但偏向高取值特征    |
| 信息增益率     | C4.5算法的特征选择依据                | 解决高取值特征偏向；但对低取值特征可能过度惩罚 |
| Gini系数       | CART算法的特征选择依据                | 计算速度快（无需对数运算）；适用于二分类/多分类 |